import { StyleSheet } from "react-native";

const appStyles = StyleSheet.create({
  imageBackground: {
    flex: 1,
    resizeMode: "cover",
    overflow: "hidden",
  },
});
export default appStyles;
